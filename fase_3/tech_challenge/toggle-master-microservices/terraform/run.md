Como executar:

1. Entrar na pasta backend e executar o terraform
cd /backend
```
terraform init 
terraform plan 
terraform apply
```

após o apply

/modules
terragrunt init --all
terragrunt apply --all
