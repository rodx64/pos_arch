name: 🚀 CI Básico

# Quando executar este workflow
on:
  push:
    branches: [ 'pos/*' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Permite execução manual

jobs:
  build-and-test:
    name: 🏗️ Build and Test
    runs-on: ubuntu-latest
    
    steps:
      # Passo 1: 
      # - Clonar o repositório
      # - Fazer checkout da branch/commit correto
      # - Garantir que o código esteja disponível para os próximos steps
      - name: 📥 Checkout código
        uses: actions/checkout@v4
      
      # Passo 2: Configurar Node.js
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: fase_3/aulas/1_ci_cd/hands_on/1_aula/1_hands_on/app/package-lock.json
      
      # Passo 3: Instalar dependências
      - name: 📦 Instalar dependências
        working-directory: fase_3/aulas/1_ci_cd/hands_on/1_aula/1_hands_on/app
        run: npm ci
      
      # Passo 4: Executar testes
      - name: 🧪 Executar testes
        working-directory: fase_3/aulas/1_ci_cd/hands_on/1_aula/1_hands_on/app
        run: npm test
      
      # Passo 5: Executar lint (análise de código)
      - name: 🔍 Lint
        working-directory: fase_3/aulas/1_ci_cd/hands_on/1_aula/1_hands_on/app
        run: npm run lint --if-present
