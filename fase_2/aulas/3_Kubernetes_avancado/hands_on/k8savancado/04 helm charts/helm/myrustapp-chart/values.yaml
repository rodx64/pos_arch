replicaCount: 2

image:
  repository: myrustapp
  tag: "1.0.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  containerPort: 8080

resources:
  limits:
    memory: 256Mi
    cpu: 500m
  requests:
    memory: 128Mi
    cpu: 250m

podAnnotations: {}

serviceAccount:
  create: false
  name: ""

rbac:
  create: false

config:
  greeting: "Olá, Helm + Rust!"
