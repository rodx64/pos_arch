apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-deployment
  namespace: toggle-master
  labels:
    app: analytics
    tier: backend
    tags.datadoghq.com/env: "dev"
    tags.datadoghq.com/service: "analytics-service"
    tags.datadoghq.com/version: "1.0.0"
spec:
  selector:
    matchLabels:
      app: analytics
      tier: backend
  template:
    metadata:
      labels:
        app: analytics
        tier: backend
        tags.datadoghq.com/env: "dev"
        tags.datadoghq.com/service: "analytics-service"
        tags.datadoghq.com/version: "1.0.0"
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8005"
        prometheus.io/path: "/metrics"
        admission.datadoghq.com/enabled: "true"
        ad.datadoghq.com/analytics-container.checks: |
          {
            "openmetrics": {
              "instances": [
                {
                  "openmetrics_endpoint": "http://%%host%%:8005/metrics",
                  "namespace": "toggle_master",
                  "metrics": [
                    "http_requests_total",
                    "http_request_duration_seconds",
                    "process_cpu_seconds_total",
                    "process_resident_memory_bytes"
                  ]
                }
              ]
            }
          }
    spec:
      containers:
        - name: analytics-container
          image: 951619424825.dkr.ecr.us-east-1.amazonaws.com/toggle-master:analytics-service-c55d61ed567c919078e3a1f41b50240b74be2ec5
          imagePullPolicy: Always
          ports:
            - containerPort: 8005
          env:
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.monitoring.svc.cluster.local:4317"
            - name: OTEL_EXPORTER_OTLP_INSECURE
              value: "true"
            - name: OTEL_LOGS_EXPORTER
              value: "otlp"
            - name: OTEL_METRICS_EXPORTER
              value: "otlp"
            - name: OTEL_TRACES_EXPORTER
              value: "otlp"
            - name: DD_LOGS_INJECTION
              value: "true"
            - name: DD_ENV
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['tags.datadoghq.com/env']
            - name: DD_SERVICE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['tags.datadoghq.com/service']
            - name: DD_VERSION
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['tags.datadoghq.com/version']
          envFrom:
            - configMapRef:
                name: analytics-config
            - secretRef:
                name: analytics-secret
          resources:
            limits:
              memory: "512Mi"
              cpu: "1000m"
            requests:
              memory: "256Mi"
              cpu: "500m"
          
          # PROBES
          startupProbe:
            httpGet:
              path: /health/startup
              port: 8005
            periodSeconds: 5 # intervalo entre as checagens
            failureThreshold: 30 # falhas antes de marcar como falha (5s * 30 = 150s total)

          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8005
            initialDelaySeconds: 5 # aguarda o startup completar
            periodSeconds: 10 # intervalo entre as checagens
            failureThreshold: 3 # falhas antes de marcar como não pronto

          livenessProbe:
            httpGet:
              path: /health/live
              port: 8005
            initialDelaySeconds: 15 # deve ser a soma de startup + readiness
            periodSeconds: 10 # intervalo entre as checagens
            failureThreshold: 3 # falhas antes de reiniciar o container
